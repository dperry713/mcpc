apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "gateway.fullname" . }}-netpol
  labels:
    app.kubernetes.io/name: {{ include "gateway.name" . }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: {{ include "gateway.name" . }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Deny all ingress by default
    []
  egress:
    # Allow DNS resolution queries to kube-dns
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: "control-plane"
